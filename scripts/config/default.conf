#!/bin/bash
# Default configuration - full installation
# Copy and customize: cp config/default.conf config/custom.conf

# Phase 1: CLI Tools
INSTALL_CLI_TOOLS=true

# Phase 2: Node.js
INSTALL_NODEJS=true
NODE_VERSION="lts"
GLOBAL_NODE_PACKAGES=(
    "typescript"
    "ts-node"
    "prettier"
    "@biomejs/biome"
)

# Phase 3: Neovim + Docker
INSTALL_NEOVIM=true
INSTALL_DOCKER=true

# Phase 4: tmux
INSTALL_TMUX=true

# Phase 5: Additional Tools
INSTALL_ADDITIONAL_TOOLS=true

# Phase 6: System Optimization
INSTALL_SYSTEM_OPTIMIZATION=true

# System settings
SWAPPINESS=10
